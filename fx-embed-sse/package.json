{
  "name": "fx-embed-sse",
  "version": "1.0.0",
  "description": "Embeddable FX rates widget (iframe + SSE) using Frankfurter API",
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch server.js --watch public --ext js,html,css --exec \"node server.js\"",
    "start": "node server.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.2.1",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.13"
  }
}